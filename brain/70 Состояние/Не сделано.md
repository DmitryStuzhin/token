---
aliases: [todo, бэклог, что осталось]
tags: [тип/состояние, статус/план]
---

# Не сделано

> Честный список. Обновлять при каждом изменении.

Технический порядок выполнения и критерии готовности: [[План технической реализации]].

## Крупное

| Что | Почему важно | Связи |
|---|---|---|
| Кабинет [[Родитель\|родителя]] | роль закрыта, таблицы `guardians` нет | [[ADR-009 Родитель как Guardian]] |
| Платежи | абонемент только уменьшается | [[Subscription]] |
| Парсер банка задач | 91 задача сгенерирована, импорт готов | [[Task]] |
| Фоновые задачи | напоминания, «просрочено», сводки | [[NotificationPref]] |
| [[Админ\|Роль администратора]] | нужна при нескольких репетиторах | [[Админ]] |
| Hosted PostgreSQL deployment | локальный cutover готов; для публикации нужен managed `DATABASE_URL` | [[Миграция SQLite в PostgreSQL]] |
| Production deployment | VPS создан; нужны публичный IPv4, домен, TLS и security-gate | [[Production-развёртывание]] |

## Среднее

- Отправка уведомлений: ни Telegram, ни почты
- Восстановление пароля и подтверждение email
- Удаление SQLite adapter — только после стабильного PostgreSQL rollback-window
- Удаление legacy `/api/state[.js]` — после периода наблюдения API v1
- TTL/retention для `idempotency_keys` — до публичной эксплуатации

## Ближайший технический этап

Этап 5 завершён. Следующий технический этап — этап 6: версионированный банк
контента и assessment, начиная с `T6-001` (`tasks` + `task_versions`).

## Исправленные ошибки

Оставлено как память — чтобы не повторить:

1. **Приглашение падало у репетитора.** Страница безусловно читала профиль
   ученика, которого у репетитора нет. Теперь роль учитывается.
2. **Ученик без репетитора попадал в тупик.** Поле ввода кода было спрятано
   в блоке «Репетитор», который рисуется только тем, у кого репетитор есть.
3. **Вкладки репетитора были якорями** `tutor.html#groups` — навигация
   «возвращалась на главную». Стали отдельными страницами.
4. **`fullState` возвращал пустой срез** — приглашения не находились.
5. **Занятие, закрытое досрочно, терялось** в посещаемости: фильтр смотрел
   на расписание, а не на статус.
6. **Коды приглашений генерировались кириллицей** и ломались в адресной строке.

## Связи

[[Риски]] · [[Масштабирование]] · [[Карта проекта]]
