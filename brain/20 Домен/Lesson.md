---
aliases: [занятие, урок]
tags: [тип/сущность, роль/репетитор, роль/ученик, статус/готово]
таблица: lessons
источник: server/db.js
---

# Lesson — занятие

> Событие в расписании. Адресуется **либо** привязке, **либо** группе.

## Поля

| поле | смысл |
|---|---|
| `enrollment_id` **или** `group_id` | кому; заполнен ровно один |
| `subject_id`, `tutor_id` | денормализовано для быстрых выборок |
| `starts_at`, `duration_min` | когда |
| `status` | `planned` · `done` · `moved` · `cancelled` · `missed` |
| `lesson_links` | упорядоченные ссылки, см. [[LessonLink]] |
| `lesson_tasks` | нормализованные задачи занятия |
| `lesson_notes` | заметки с автором и видимостью |
| `version` | optimistic concurrency; увеличивается при каждой записи |

Автомат запрещает открывать `done`, `cancelled` и `missed` повторно. После
`moved` разрешены новое планирование или отмена.

## Что происходит при закрытии

`POST /api/lessons/:id/status` со `status: done`:
пишется [[LessonAttendance]] всем участникам и списывается занятие
с [[Subscription|абонемента]].

## Тонкость учёта

Занятие попадает в посещаемость, если оно **уже прошло или явно закрыто**
репетитором. Статус важнее расписания — иначе закрытое досрочно занятие
терялось бы в статистике.

## Связи

[[LessonLink]] · [[LessonAttendance]] · [[Task]] · [[TaskAttempt]] ·
[[Проведение занятия]] · [[Живой канал]] · [[Экран Занятие]] · [[Модульные границы]]
