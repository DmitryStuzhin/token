/* Выполняется до бандла редактора: список @font-face строится один раз при
   инициализации, и к этому моменту путь уже должен быть известен. Инлайн-скрипт
   здесь запрещён CSP, поэтому отдельный модуль — модули идут строго по порядку. */
window.EXCALIDRAW_ASSET_PATH = '/vendor/';
